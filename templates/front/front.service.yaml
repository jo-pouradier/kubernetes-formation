{{- if .Values.front.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{.Release.Name}}-front-service
spec:
  selector:
    app: front-{{.Release.Name}}
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
{{- end }}